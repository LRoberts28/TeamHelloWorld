import java.util.ArrayList;
public class TempArrays {
    private static ArrayList<Object> allUsers; //replace object with user 
    public TempArrays()
    {
        //when User class is finished, edit to add a bunch of users
    }
    public static void addUser(Object newUser) //replace object with user
    {
        allUsers.add(newUser);

    }
    //implement removeUser(String email), getUser(String email),  
}
